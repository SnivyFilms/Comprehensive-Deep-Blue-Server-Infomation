On further analyzation, when Mimicry came out, the ability to use V to talk to humans has been removed from dog (as everyone knows), it turns out that it's just gone entirely.
SCP-049 cannot use "V" to talk to humans, mbad

Server info page in game has been updated. Added a discord link and added a list of plugins that the server uses as a transparency measure for all players so they know what they are getting into when the server says “modded”

Changed the chances on getting items with coins, basically it was way too easy to get high level stuff, so that has been changed, enjoy your medkits and scientist keycards way more often. Also you no longer can get Guard Cards from 914 with a coin, and instead you can get Zone Manager cards